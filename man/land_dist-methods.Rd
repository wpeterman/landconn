% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifc-methods.R
\name{land_dist-methods}
\alias{land_dist-methods}
\alias{print.land_dist}
\alias{summary.land_dist}
\alias{plot.land_dist}
\title{Methods for \code{land_dist} distance matrices}
\usage{
\method{print}{land_dist}(x, ...)

\method{summary}{land_dist}(object, ...)

\method{plot}{land_dist}(x, ...)
}
\arguments{
\item{x, object}{An object of class \code{land_dist}, as returned by \code{\link[=dist_mat]{dist_mat()}}.}

\item{...}{Additional arguments passed to \code{\link[graphics:image]{graphics::image()}} (for \code{plot()}) or
ignored (for \code{print()} and \code{summary()}).}
}
\value{
\code{print()} returns its input invisibly. \code{summary()} invisibly returns the
distribution of the unique pairwise distances (the lower triangle) and prints it.
\code{plot()} draws a heatmap of the matrix and returns \code{NULL} invisibly.
}
\description{
Display, summarize, and plot the square distance matrix returned by \code{\link[=dist_mat]{dist_mat()}}.
}
\details{
The diagonal of a \code{land_dist} matrix is zero (each site is zero distance from
itself). \code{summary()} reports only the lower triangle, which holds each pairwise
distance once, so the statistics describe the spread of distances between distinct
sites.
}
\examples{
set.seed(123)
loc <- data.frame(x = runif(10, 0, 10), y = runif(10, 0, 10))
d <- dist_mat(loc)

d              # print method
summary(d)     # spread of pairwise distances
plot(d)        # heatmap

}
\seealso{
\code{\link[=dist_mat]{dist_mat()}}, \code{\link[=lower]{lower()}}
}
