% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifc_optim.R
\name{ifc_optim}
\alias{ifc_optim}
\title{Optimize the incidence function scale parameter (alpha)}
\usage{
ifc_optim(
  formula,
  data,
  dist_mat,
  model = 1,
  family = stats::binomial(),
  patch_area = NULL,
  occ_sites = NULL,
  area_transform = "none",
  alpha_range = NULL,
  n_grid = 50,
  n_boot = 0,
  conf_level = 0.95
)
}
\arguments{
\item{formula}{A model formula for the GLM. The predictor named \code{connectivity} is
reserved: it is recomputed by \code{\link[=ifc]{ifc()}} at each candidate \code{alpha} and added to the
data, so it does not need to exist in \code{data}. Any other terms must be columns of
\code{data}. For example \code{occupied ~ connectivity + habitat}.}

\item{data}{A data frame with one row per site, in the same order as the rows of
\code{dist_mat}. It holds the response and any covariates in \code{formula} (but not
\code{connectivity}, which is generated internally).}

\item{dist_mat}{A square pairwise distance matrix, such as the output of \code{\link[=dist_mat]{dist_mat()}}.}

\item{model}{Numeric, one or more values in 1-4, the \code{\link[=ifc]{ifc()}} model(s) used to build
connectivity. See \code{\link[=ifc]{ifc()}} for the four formulations. (Default = 1). A single value
returns one fit; a vector (for example \code{1:4}) fits the best \code{alpha} for each model
and returns a ranked model selection table. See Value and Details.}

\item{family}{A GLM family, as a family object, a family function, or its name (for
example \code{binomial}, \code{poisson}, \code{gaussian}). (Default = \code{binomial()})}

\item{patch_area, occ_sites}{Optional numeric vectors passed through to \code{\link[=ifc]{ifc()}} (patch
areas for Models 2-4, and a 0/1 occupancy vector to restrict contributors). Each
must be equal in length to the number of sites. See \code{\link[=ifc]{ifc()}}.}

\item{area_transform}{Passed to \code{\link[=ifc]{ifc()}}: \code{"none"} (Default) uses patch area directly,
\code{"log"} uses \code{log(patch_area)}. You may supply both, \code{c("none", "log")}, to compare
them for Models 2-4 in a model selection table (Model 1 ignores patch area, so it
appears once). See \code{\link[=ifc]{ifc()}}.}

\item{alpha_range}{Numeric length-2 vector giving the lower and upper bounds of the
\code{alpha} search, in the same distance units as \code{dist_mat}. If \code{NULL} (Default), it is
set from the spread of pairwise distances. If the estimate lands at a bound, widen
this range and refit.}

\item{n_grid}{Number of log-spaced \code{alpha} values in the profile grid (Default = 50).
The grid both seeds the optimizer and sets the resolution of the profile CI; raise
it for a finer profile.}

\item{n_boot}{Number of parametric bootstrap replicates for the \code{alpha} confidence
interval (Default = 0, no bootstrap). Each replicate simulates a new response from
the fitted model, holding the site network fixed, and re-optimizes \code{alpha}. See
Details.}

\item{conf_level}{Confidence level for the \code{alpha} interval(s) (Default = 0.95).}
}
\value{
When a single model and area transform are requested, an object of class
\code{ifc_optim}. When several are requested (a vector \code{model}, or \code{area_transform = c("none", "log")} with Models 2-4), an object of class \code{ifc_optim_set}: a list with
\code{fits} (the named list of individual \code{ifc_optim} fits), \code{table} (a ranked
\code{ifc_modsel} model selection table, see \code{\link[=ifc_modsel]{ifc_modsel()}}), and \code{best} (the top-ranked
\code{ifc_optim} fit). See \code{\link[=print.ifc_optim_set]{print.ifc_optim_set()}}.

A single \code{ifc_optim} object is a list with elements:
\describe{
\item{\code{alpha}}{The estimated scale parameter, in the distance units of \code{dist_mat}.
Larger values mean connection probability decays more slowly with distance, so
even distant sites stay connected.}
\item{\code{alpha_ci}}{The profile likelihood confidence interval for \code{alpha} (the
range of grid \code{alpha} values within \code{qchisq(conf_level, 1) / 2} log-likelihood
units of the maximum). Its resolution is the grid; raise \code{n_grid} to sharpen it.}
\item{\code{alpha_ci_boot}}{The bootstrap percentile interval for \code{alpha}, or \code{NULL}
if \code{n_boot = 0}.}
\item{\code{boot}}{The vector of bootstrap \code{alpha} estimates, or \code{NULL}.}
\item{\code{logLik}, \code{AIC}}{The maximized log-likelihood and the AIC of the best model.
\code{AIC} counts one extra parameter for the estimated \code{alpha}, so it is comparable
across \code{\link[=ifc]{ifc()}} models and to a null (connectivity-free) model.}
\item{\code{model}}{The fitted \code{glm} object at \code{alpha}. Treat its coefficients and
standard errors as conditional on \code{alpha} (see Details).}
\item{\code{profile}}{A data frame of \code{alpha} and \code{logLik} over the grid, used by
\code{plot()}.}
\item{\code{boundary}}{\code{TRUE} if \code{alpha} sits at a bound of \code{alpha_range}, a sign the
range should be widened.}
}
Use \code{\link[=print.ifc_optim]{print.ifc_optim()}}, \code{\link[=summary.ifc_optim]{summary.ifc_optim()}}, and \code{\link[=plot.ifc_optim]{plot.ifc_optim()}} to read it.
}
\description{
Estimate the dispersal scale \code{alpha} by finding the value whose incidence
function connectivity best predicts a response in a generalized linear model
(GLM). This is a scale of effect optimization: \code{alpha} is chosen to maximize the
fit of \code{glm(formula, family)}, where one predictor is the connectivity produced by
\code{\link[=ifc]{ifc()}}.
}
\details{
\code{alpha} enters \code{\link[=ifc]{ifc()}} nonlinearly, so it cannot be read from a single GLM. For each
candidate \code{alpha}, connectivity is recomputed, the GLM is fit, and its
log-likelihood is recorded. The \code{alpha} that maximizes the log-likelihood is the
estimate. This is a \emph{profile likelihood}: a coarse log-spaced grid locates the peak
and \code{\link[stats:optimize]{stats::optimize()}} refines it.

\strong{How to read the result.} Start with the profile plot. A sharp peak means
\code{alpha} is well identified; a flat profile means the data carry little information
about the scale, and the estimate should be treated with caution. The \code{alpha}
estimate is a distance: it is the scale over which connection probability falls to
\code{exp(-1)}, about 0.37, of its maximum.

\strong{Uncertainty.} Two intervals are available. The profile likelihood interval
is fast and comes from the curvature of the profile. The parametric bootstrap
(\code{n_boot > 0}) simulates new responses from the fitted model and re-optimizes
\code{alpha} for each, giving a percentile interval. The bootstrap is parametric on
purpose: connectivity is a whole-network quantity, so resampling sites would break
the network (duplicated sites sit at zero distance). Holding the network fixed and
resampling only the stochastic response is the coherent choice.

\strong{Caveats.} The GLM treats connectivity as a fixed predictor, so the GLM's own
standard errors and p-values for the \code{connectivity} coefficient are conditional on
the estimated \code{alpha} and are mildly anticonservative. If the response and
\code{occ_sites} are the same occupancy data, you are predicting occupancy partly from
itself; avoid that.

\strong{Comparing models.} Passing several models (or both area transforms) fits the
best \code{alpha} for each and ranks them in a model selection table. The table counts the
number of parameters (\code{K}) correctly: the GLM coefficients, the dispersion parameter
for families that estimate one (such as \code{gaussian}), and one more for the optimized
\code{alpha}. Ranking is by AICc, with delta values and Akaike weights. The same parameter
counting is used by the \code{\link[=AIC]{AIC()}}, \code{\link[=BIC]{BIC()}}, and \code{\link[=AICc]{AICc()}} methods for \code{ifc_optim}
objects, so a fit compares correctly against an ordinary \code{glm} with no connectivity
term. See \code{\link[=ifc_modsel]{ifc_modsel()}}.
}
\examples{
set.seed(1)

## Simulate sites whose response depends on connectivity at a known alpha = 4
loc  <- data.frame(x = runif(120, 0, 20), y = runif(120, 0, 20))
d    <- dist_mat(loc)
conn <- as.numeric(ifc(4, d, model = 1, scale = TRUE))
dat  <- data.frame(abundance = 2 + 8 * conn + rnorm(120, 0, 0.5))

## Recover alpha (a continuous response identifies the scale well)
fit <- ifc_optim(abundance ~ connectivity, data = dat,
                 dist_mat = d, model = 1, family = gaussian)
fit
summary(fit)
plot(fit)

## Compare ifc models 1-4 (and both area transforms) in one call
p_a <- runif(120, 5, 50)
fits <- ifc_optim(abundance ~ connectivity, data = dat, dist_mat = d,
                  model = 1:4, patch_area = p_a,
                  area_transform = c("none", "log"), family = gaussian)
fits                 # ranked model selection table
fits$best            # the top-ranked single fit

\donttest{
## Add a parametric bootstrap confidence interval for alpha
fit_b <- ifc_optim(abundance ~ connectivity, data = dat,
                   dist_mat = d, model = 1, family = gaussian,
                   n_boot = 200)
fit_b$alpha_ci_boot
}

}
\seealso{
\code{\link[=ifc]{ifc()}}, \code{\link[=dist_mat]{dist_mat()}}, \code{\link[=ifc_modsel]{ifc_modsel()}}, \code{\link[=AICc]{AICc()}}
}
\author{
Bill Peterman \href{mailto:peterman.73@osu.edu}{peterman.73@osu.edu}
}
