% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifc-methods.R
\name{ifc-methods}
\alias{ifc-methods}
\alias{print.ifc}
\alias{summary.ifc}
\alias{plot.ifc}
\title{Methods for \code{ifc} connectivity objects}
\usage{
\method{print}{ifc}(x, ...)

\method{summary}{ifc}(object, ...)

\method{plot}{ifc}(x, sort = TRUE, ...)
}
\arguments{
\item{x, object}{An object of class \code{ifc}, as returned by \code{\link[=ifc]{ifc()}}.}

\item{...}{Additional arguments passed to the underlying \code{\link[graphics:barplot]{graphics::barplot()}}
(for \code{plot()}) or ignored (for \code{print()} and \code{summary()}).}

\item{sort}{Logical (Default = TRUE). If TRUE, \code{plot()} orders sites from most to
least connected, which makes the ranking easy to read. If FALSE, sites are shown
in their original order.}
}
\value{
\code{print()} returns its input invisibly. \code{summary()} invisibly returns a list with
the model context and the distribution of connectivity values, and prints a
readable summary as a side effect. \code{plot()} draws a barplot and returns the bar
midpoints invisibly.
}
\description{
Display, summarize, and plot the output of \code{\link[=ifc]{ifc()}}. An \code{ifc} object is a named
numeric vector of relative connectivity values (one per site) that also records
which model produced it.
}
\details{
Connectivity values are \emph{relative}: they are only meaningful in comparison with
one another within the same call, not as absolute quantities. A site with twice
the value of another is roughly twice as connected to the rest of the network
under the chosen model. If the object was produced with \code{scale = TRUE}, values run
from 0 to 1, where 1 is the most connected site.
}
\examples{
set.seed(123)
loc <- data.frame(x = runif(10, 0, 10), y = runif(10, 0, 10))
d <- dist_mat(loc)
conn <- ifc(alpha = 3, dist_mat = d, model = 1)

conn            # print method
summary(conn)   # distribution and most/least connected sites
plot(conn)      # sorted barplot

}
\seealso{
\code{\link[=ifc]{ifc()}}
}
