% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aic-methods.R
\name{AICc}
\alias{AICc}
\alias{AICc.default}
\title{Second-order Akaike information criterion (AICc)}
\usage{
AICc(object, ...)

\method{AICc}{default}(object, ...)
}
\arguments{
\item{object}{A fitted model object with a \code{logLik} method.}

\item{...}{Ignored.}
}
\value{
The AICc value as a single number, or \code{NA} when \code{n - K - 1 <= 0} (too few
observations for the correction to be defined).
}
\description{
Small-sample corrected AIC, \eqn{AICc = AIC + 2K(K + 1) / (n - K - 1)}, where \code{K} is
the number of parameters and \code{n} the number of observations. The default method works
for any model with a \code{\link[=logLik]{logLik()}} method, including \code{\link[=ifc_optim]{ifc_optim()}} fits (for which \code{K}
includes the optimized \code{alpha}), \code{glm}, and \code{lm}.
}
\examples{
set.seed(1)
loc  <- data.frame(x = runif(120, 0, 20), y = runif(120, 0, 20))
d    <- dist_mat(loc)
conn <- as.numeric(ifc(4, d, model = 1, scale = TRUE))
dat  <- data.frame(abundance = 2 + 8 * conn + rnorm(120, 0, 0.5))
fit  <- ifc_optim(abundance ~ connectivity, data = dat, dist_mat = d,
                  model = 1, family = gaussian)
AICc(fit)
}
\seealso{
\code{\link[=AIC.ifc_optim]{AIC.ifc_optim()}}, \code{\link[=ifc_modsel]{ifc_modsel()}}
}
