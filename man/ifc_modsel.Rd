% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aic-methods.R
\name{ifc_modsel}
\alias{ifc_modsel}
\alias{print.ifc_modsel}
\title{Model selection table for \code{ifc_optim} fits}
\usage{
ifc_modsel(..., rank = c("AICc", "AIC", "BIC"))

\method{print}{ifc_modsel}(x, ...)
}
\arguments{
\item{...}{Two or more \code{ifc_optim} objects, a single named list of them, or an
\code{ifc_optim_set}.}

\item{rank}{The information criterion to rank by: \code{"AICc"} (Default), \code{"AIC"}, or
\code{"BIC"}. \code{delta} and \code{weight} are computed from the chosen criterion.}

\item{x}{An \code{ifc_modsel} table.}
}
\value{
A data frame of class \code{ifc_modsel}, one row per model, sorted best first, with
columns: \code{model} (label), \code{K} (parameter count), \code{alpha}, \code{logLik}, \code{AIC}, \code{AICc},
\code{BIC}, \code{delta} (difference from the best model on the ranking criterion), and
\code{weight} (Akaike weight). Models within about 2 \code{delta} units of the best have
comparable support.
}
\description{
Rank two or more \code{\link[=ifc_optim]{ifc_optim()}} fits (or the fits inside an \code{ifc_optim_set}) by an
information criterion, with delta values and Akaike weights. The number of parameters
(\code{K}) counts the GLM coefficients, any dispersion parameter, and the optimized \code{alpha}.
}
\details{
Comparisons assume the fits share the same response and data. The function
warns if the number of observations differs across fits. Weights are
\eqn{w_i = \exp(-\Delta_i / 2) / \sum_j \exp(-\Delta_j / 2)} and sum to 1.
}
\examples{
set.seed(1)
loc  <- data.frame(x = runif(120, 0, 20), y = runif(120, 0, 20))
d    <- dist_mat(loc)
conn <- as.numeric(ifc(4, d, model = 1, scale = TRUE))
dat  <- data.frame(abundance = 2 + 8 * conn + rnorm(120, 0, 0.5))
p_a  <- runif(120, 5, 50)

m1 <- ifc_optim(abundance ~ connectivity, data = dat, dist_mat = d, model = 1,
                family = gaussian)
m2 <- ifc_optim(abundance ~ connectivity, data = dat, dist_mat = d, model = 2,
                patch_area = p_a, family = gaussian)
ifc_modsel(m1, m2)
}
\seealso{
\code{\link[=ifc_optim]{ifc_optim()}}, \code{\link[=AICc]{AICc()}}, \code{\link[=AIC.ifc_optim]{AIC.ifc_optim()}}
}
