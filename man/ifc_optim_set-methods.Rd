% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifc_optim_set-methods.R
\name{ifc_optim_set-methods}
\alias{ifc_optim_set-methods}
\alias{print.ifc_optim_set}
\alias{summary.ifc_optim_set}
\alias{plot.ifc_optim_set}
\title{Methods for \code{ifc_optim_set} model selection objects}
\usage{
\method{print}{ifc_optim_set}(x, ...)

\method{summary}{ifc_optim_set}(object, ...)

\method{plot}{ifc_optim_set}(x, ...)
}
\arguments{
\item{x, object}{An \code{ifc_optim_set} object.}

\item{...}{Additional arguments passed to the underlying plotting calls (for \code{plot()})
or ignored.}
}
\value{
\code{print()} and \code{summary()} return their input invisibly after printing the model
selection table; \code{summary()} also prints the best fit. \code{plot()} overlays the \code{alpha}
log-likelihood profiles of all models and returns \code{NULL} invisibly.
}
\description{
An \code{ifc_optim_set} is returned by \code{\link[=ifc_optim]{ifc_optim()}} when several \code{\link[=ifc]{ifc()}} models (or both
area transforms) are compared. It holds the individual fits, a ranked model selection
table, and the best fit.
}
\details{
The best model is the top row of the table (lowest AICc by default). Drill into it
with \code{object$best}, which is an ordinary \code{\link[=ifc_optim]{ifc_optim()}} object with its own \code{print},
\code{summary}, and \code{plot} methods, or pull the table with \code{object$table}. See
\code{\link[=ifc_modsel]{ifc_modsel()}} for the table columns.
}
\examples{
set.seed(1)
loc  <- data.frame(x = runif(120, 0, 20), y = runif(120, 0, 20))
d    <- dist_mat(loc)
conn <- as.numeric(ifc(4, d, model = 1, scale = TRUE))
dat  <- data.frame(abundance = 2 + 8 * conn + rnorm(120, 0, 0.5))
p_a  <- runif(120, 5, 50)

fits <- ifc_optim(abundance ~ connectivity, data = dat, dist_mat = d,
                  model = 1:4, patch_area = p_a, family = gaussian)
fits
plot(fits)
fits$best

}
\seealso{
\code{\link[=ifc_optim]{ifc_optim()}}, \code{\link[=ifc_modsel]{ifc_modsel()}}
}
